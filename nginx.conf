http {
        upstream backend {
                server <server1_ip>:<port>;
                server <server1_ip>:<port>;
        }
 
        server {
                listen 80;
                location / {
                        proxy_pass http://backend;
                }
        }
}
